{
    "version": 2,
    "buildCommand": "npm run build",
    "builds": [
        {
            "src": "./dist/index.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/dist/index.ts"
        }
    ]
}